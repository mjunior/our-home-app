---
phase: 12-fundacao-de-autenticacao-email-senha
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-02"
files_modified:
  - src/main.tsx
  - src/app/foundation/runtime.ts
  - src/app/auth/login-page.tsx
  - src/app/auth/register-page.tsx
  - tests/e2e/auth-flow.spec.ts
autonomous: false
requirements:
  - AUTH-01
  - AUTH-03
user_setup:
  - Validar manualmente no navegador o refresh da pagina mantendo sessao ativa e o redirecionamento automatico de `/login` para area autenticada.
must_haves:
  truths:
    - Usuario consegue cadastrar em `/n-account` e logar em `/login` com feedback claro.
    - Sessao autenticada permanece apos refresh e libera acesso ao shell principal.
    - Usuario autenticado acessando `/login` e redirecionado para a area principal.
  artifacts:
    - src/main.tsx
    - src/app/auth/login-page.tsx
    - src/app/auth/register-page.tsx
    - src/app/foundation/runtime.ts
    - tests/e2e/auth-flow.spec.ts
  key_links:
    - Telas de auth consomem endpoints `/api/auth/*` e propagam sessao para runtime foundation.
    - Bootstrap do app consulta sessao atual (`/api/auth/me`) para decidir entre shell autenticado e telas anonimas.
---

<objective>
Entregar a integracao frontend de autenticacao com rotas `/login` e `/n-account` e bootstrap por sessao.

Purpose: fechar o fluxo ponta a ponta para cadastro/login e persistencia de sessao no cliente.
Output: UX funcional de entrada, redirecionamentos basicos e cobertura de fluxo de autenticacao no frontend.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
@./.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-fundacao-de-autenticacao-email-senha/12-CONTEXT.md
@.planning/phases/12-fundacao-de-autenticacao-email-senha/12-02-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Criar telas de login/cadastro funcionais em `/login` e `/n-account`</name>
  <files>src/main.tsx, src/app/auth/login-page.tsx, src/app/auth/register-page.tsx, tests/e2e/auth-flow.spec.ts</files>
  <behavior>
    - `/login` permite autenticar com email/senha e mostra erro neutro para credenciais invalidas.
    - `/n-account` permite cadastro com email/senha e trata email duplicado sem revelar detalhes.
    - Apos login ou cadastro com sucesso, usuario e direcionado para area principal autenticada.
  </behavior>
  <action>Adicionar roteamento client-side minimo baseado em `window.location.pathname` para suportar `/login` e `/n-account` sem introduzir router completo nesta fase. Implementar formularios controlados com feedback via snackbar e chamadas para `/api/auth/login` e `/api/auth/register` (ou endpoint equivalente definido no plano 02). Cobrir fluxo feliz e erro principal em teste e2e/jsdom.</action>
  <verify><automated>npm run test:e2e -- tests/e2e/auth-flow.spec.ts</automated></verify>
  <done>Usuario consegue completar login e cadastro nas rotas exigidas com feedback consistente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hidratacao de sessao no bootstrap e guardas basicos de acesso autenticado</name>
  <files>src/main.tsx, src/app/foundation/runtime.ts, tests/e2e/auth-flow.spec.ts</files>
  <behavior>
    - App consulta sessao atual ao iniciar e restaura estado autenticado apos refresh.
    - Usuario nao autenticado nao entra no shell foundation.
    - Usuario autenticado em `/login` e redirecionado para rota principal autenticada.
  </behavior>
  <action>Implementar bootstrap assincrono em `main.tsx` que consulta `/api/auth/me`, configura contexto de sessao (incluindo `householdId`) e bloqueia render do shell enquanto resolve autenticacao. Adaptar `runtime.ts` para consumir `householdId` da sessao em vez de hardcode quando em modo API. Atualizar testes cobrindo refresh com sessao valida e redirecionamento de rota anonima para area autenticada.</action>
  <verify><automated>npm run test:e2e -- tests/e2e/auth-flow.spec.ts</automated></verify>
  <done>Persistencia de sessao no cliente funciona e fluxo de entrada esta protegido por guardas basicos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fluxo completo de autenticacao no frontend com cadastro, login e sessao persistente</what-built>
  <how-to-verify>Acesse `/n-account`, crie um usuario novo, confirme redirecionamento para area principal e recarregue a pagina. Em seguida abra `/login` autenticado e valide redirecionamento automatico. Faca logout, tente login com senha errada e confirme mensagem `Credenciais invalidas` sem detalhes adicionais.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
- [ ] npm run test:e2e -- tests/e2e/auth-flow.spec.ts
- [ ] npm run lint
- [ ] Human verification approved
</verification>

<success_criteria>
- AUTH-01 coberto por cadastro funcional em `/n-account`.
- AUTH-03 coberto por sessao persistente apos refresh.
- Fase 12 pronta para isolamento por `userId` na fase 13.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fundacao-de-autenticacao-email-senha/12-03-SUMMARY.md`
</output>
