---
phase: 12-fundacao-de-autenticacao-email-senha
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - package.json
  - src/server/vite-api.ts
  - src/modules/auth/auth.service.ts
  - src/modules/auth/session-token.ts
  - tests/modules/auth-api.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - Cadastro por email/senha cria conta e sessao inicial sem vazar informacoes sensiveis.
    - Login com credenciais validas cria sessao em cookie HttpOnly reutilizavel apos refresh.
    - Credenciais invalidas retornam mensagem unica e neutra sem revelar se email existe.
    - Logout encerra apenas a sessao atual limpando cookie no cliente.
  artifacts:
    - src/server/vite-api.ts
    - src/modules/auth/session-token.ts
    - src/modules/auth/auth.service.ts
    - tests/modules/auth-api.test.ts
  key_links:
    - Endpoint de cadastro usa AuthService.register e transforma conflitos de email em erro generico 409.
    - Endpoint de login emite JWT assinado e envia cookie com `HttpOnly` e `SameSite` conforme decisoes da fase.
    - Middleware de sessao le cookie, valida token e injeta contexto autenticado para handlers de auth.
---

<objective>
Expor contratos HTTP de autenticacao e sessao (register/login/logout/me) com politica de cookie/JWT definida em contexto.

Purpose: transformar o dominio de auth em fluxo real autenticado no backend.
Output: endpoints de auth com erro padronizado, sessao persistente e testes de API cobrindo cenarios positivos/negativos.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-fundacao-de-autenticacao-email-senha/12-CONTEXT.md
@.planning/phases/12-fundacao-de-autenticacao-email-senha/12-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implementar emissao/validacao de JWT com cookie de sessao de 7 dias</name>
  <files>package.json, src/modules/auth/session-token.ts, src/modules/auth/auth.service.ts, tests/modules/auth-api.test.ts</files>
  <behavior>
    - Login valido retorna sucesso e define cookie de sessao `HttpOnly` com expiracao de 7 dias.
    - Token contem claims minimas (`sub`, `householdId`, `exp`, `iat`) e assinatura valida.
    - Ambiente dev funciona sem `Secure`; producao exige `Secure` automaticamente.
  </behavior>
  <action>Adicionar dependencia JWT ESM-safe (ex.: `jose`) e criar modulo `session-token` para sign/verify HMAC. Integrar no `AuthService.authenticate` para gerar sessao stateless e prover helper para extrair contexto autenticado. Cobrir testes de expiracao, assinatura invalida e politica de cookie conforme contexto.</action>
  <verify><automated>npm run test -- tests/modules/auth-api.test.ts</automated></verify>
  <done>Sessao JWT em cookie funciona com contrato de seguranca e expiracao definidos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar endpoints `/api/auth/register`, `/api/auth/login`, `/api/auth/logout`, `/api/auth/me` e padronizar erros</name>
  <files>src/server/vite-api.ts, src/modules/auth/auth.service.ts, tests/modules/auth-api.test.ts</files>
  <behavior>
    - `/api/auth/register` cria conta com baseline e trata conflito de email com mensagem generica 409.
    - `/api/auth/login` autentica e responde mensagem generica em falha.
    - `/api/auth/logout` limpa apenas cookie da sessao atual.
    - `/api/auth/me` retorna usuario autenticado com base no cookie atual.
  </behavior>
  <action>Expandir `vite-api.ts` com rotas de auth, parser de cookie e mapeamento de erros para `401/409` neutros (`Credenciais invalidas` e mensagem generica para cadastro duplicado). Garantir que respostas jamais retornem hash/senha e que `logout` invalide apenas a sessao corrente via limpeza de cookie, sem revogacao global (multiplas sessoes permitidas nesta fase).</action>
  <verify><automated>npm run test -- tests/modules/auth-api.test.ts</automated></verify>
  <done>Fluxos HTTP de login/logout/me estao disponiveis com seguranca e mensagens padronizadas.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/modules/auth-api.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
 - AUTH-01 coberto no contrato HTTP com cadastro em endpoint dedicado.
- AUTH-02 coberto com login funcional por email/senha.
- AUTH-03 coberto com sessao persistida por cookie JWT de 7 dias.
- AUTH-04 coberto com erros padronizados e sem leaking de informacao sensivel.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fundacao-de-autenticacao-email-senha/12-02-SUMMARY.md`
</output>
