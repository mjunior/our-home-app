---
phase: 01-fundacao-financeira
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/
  - src/domain/accounts/account.entity.ts
  - src/domain/cards/credit-card.entity.ts
  - src/domain/categories/category.entity.ts
  - src/domain/shared/money.ts
  - tests/domain/money.test.ts
autonomous: true
requirements:
  - ACCT-01
  - ACCT-02
  - CARD-01
  - CAT-01
user_setup: []
must_haves:
  truths:
    - Account, card, and category entities exist with explicit financial fields.
    - Monetary calculations use decimal-safe primitives.
    - Database schema supports household-scoped uniqueness constraints.
  artifacts:
    - prisma/schema.prisma
    - src/domain/accounts/account.entity.ts
    - src/domain/cards/credit-card.entity.ts
    - src/domain/categories/category.entity.ts
    - src/domain/shared/money.ts
  key_links:
    - Prisma models map directly to typed domain entities.
    - Money value object is reused across account/card/category modules.
---

<objective>
Create the financial foundation model layer and schema contracts required by Phase 1.

Purpose: guarantee that all later account/card/category flows run on consistent data rules.
Output: schema, domain entities, and money utilities with tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-fundacao-financeira/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define schema and invariants for accounts, cards, and categories</name>
  <files>prisma/schema.prisma, prisma/migrations/</files>
  <behavior>
    - Account has required name, type, opening_balance, and household_id.
    - CreditCard has required name, close_day, due_day, and household_id.
    - Category enforces normalized uniqueness per household.
  </behavior>
  <action>Create or update Prisma models for Account, CreditCard, Category, and minimal Transaction anchor relation. Add database constraints for close_day/due_day ranges and household-scoped unique indexes. Generate migration files with deterministic names. Do not add Open Finance or multi-user permission tables yet.</action>
  <verify><automated>npm run prisma:validate || npx prisma validate</automated></verify>
  <done>Schema compiles, migration files generated, and constraints match behavior list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement domain entities and money value object</name>
  <files>src/domain/accounts/account.entity.ts, src/domain/cards/credit-card.entity.ts, src/domain/categories/category.entity.ts, src/domain/shared/money.ts, tests/domain/money.test.ts</files>
  <behavior>
    - Money arithmetic is decimal-safe and produces fixed two-decimal outputs.
    - Category name normalization is stable and idempotent.
    - Card cycle values reject out-of-range days.
  </behavior>
  <action>Create strongly typed domain entities with constructor guards and normalization helpers. Implement Money helpers using decimal.js and expose add/subtract/compare operations. Add unit tests for precision and normalization edge cases. Avoid using JavaScript number arithmetic for persisted amounts.</action>
  <verify><automated>npm test -- tests/domain/money.test.ts</automated></verify>
  <done>Entities enforce invariants, money helpers are test-covered, and invalid cycle/category inputs fail fast.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run prisma:validate || npx prisma validate`
- [ ] `npm test -- tests/domain/money.test.ts`
- [ ] Migration and domain files compile without type errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No float-based money arithmetic in foundation layer
- Core schema and domain invariants for ACCT-01/02, CARD-01, CAT-01 are in place
  </success_criteria>

<output>
After completion, create `.planning/phases/01-fundacao-financeira/01-01-SUMMARY.md`
</output>
