---
phase: 25-reajuste-de-fatura-de-cartao
plan: "03"
type: execute
wave: 3
depends_on:
  - "25-02"
files_modified:
  - src/app/foundation/cards/page.tsx
  - tests/e2e/cards-flow.spec.ts
autonomous: true
requirements:
  - CCADJ-01
  - CCADJ-05
user_setup: []
must_haves:
  truths:
    - Usuario consegue iniciar reajuste a partir de uma fatura visivel na tela de cartoes.
    - Usuario informa valor real da fatura, mes da fatura e data do lancamento.
    - Ao salvar, a lista e o detalhe da fatura atualizam o total para o valor real informado.
    - A transacao criada fica vinculada ao cartao e a fatura do mes escolhido.
  artifacts:
    - "src/app/foundation/cards/page.tsx: acao e Sheet/formulario de reajuste de fatura"
    - "tests/e2e/cards-flow.spec.ts: cobertura do fluxo de usuario em jsdom"
  key_links:
    - "src/app/foundation/cards/page.tsx -> src/app/foundation/runtime.ts via `invoicesController.createCreditCardAdjustment`"
    - "tests/e2e/cards-flow.spec.ts -> src/app/foundation/cards/page.tsx via fluxo de ajuste positivo e negativo"
    - "tests/e2e/cards-flow.spec.ts -> src/modules/transactions/transactions.repository.ts via assert em item `REAJUSTE` da fatura"
---

<objective>
Adicionar a experiencia inicial de reajuste na tela de cartoes.

Purpose: permitir que o usuario sincronize uma fatura de cartao com o valor real sem calcular manualmente a diferenca.
Output: botao por fatura, sheet de reajuste, chamada ao runtime e teste e2e do fluxo positivo.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-reajuste-de-fatura-de-cartao/25-02-SUMMARY.md
@src/app/foundation/cards/page.tsx
@src/app/foundation/runtime.ts
@src/lib/utils.ts
@tests/e2e/cards-flow.spec.ts
</context>

<interfaces>
Runtime method available after Plan 02:
```typescript
invoicesController.createCreditCardAdjustment({
  householdId,
  cardId,
  realInvoiceTotal,
  dueMonth,
  occurredAt,
});
```

Currency helpers:
```typescript
formatCurrencyInputBRL(raw: string, options?: { allowNegative?: boolean }): string
currencyInputToDecimal(raw: string, options?: { allowNegative?: boolean }): string
```
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Adicionar entrada de reajuste nas faturas</name>
  <files>src/app/foundation/cards/page.tsx, tests/e2e/cards-flow.spec.ts</files>
  <behavior>
    - Cada fatura na lista exibe acao acessivel `Reajustar fatura [cartao]`.
    - Acao abre formulario para a fatura selecionada e preserva selecao de detalhe.
    - Acoes existentes de pagar/desfazer pagamento e adicionar despesa continuam funcionando.
  </behavior>
  <action>Adicionar botao secundario `Reajustar` em cada item de `monthlyInvoices.cards`, com `aria-label` especifico, sem criar novo card dentro do card de lista. Ao clicar, impedir propagacao, selecionar a fatura correspondente, abrir o Sheet de reajuste e preencher estado inicial com cardId, cardName, total atual e `selectedDueMonth`. Manter a area de pagamento existente e evitar sobreposicao em mobile usando `flex-wrap`.</action>
  <verify><automated>npm run test -- tests/e2e/cards-flow.spec.ts</automated></verify>
  <done>Usuario tem ponto de entrada claro para reajustar qualquer fatura mensal existente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar sheet de reajuste de fatura</name>
  <files>src/app/foundation/cards/page.tsx, tests/e2e/cards-flow.spec.ts</files>
  <behavior>
    - Sheet mostra cartao, mes da fatura e total atual no app.
    - Formulario possui campos `Valor real da fatura`, `Mes da fatura` e `Data do reajuste`.
    - Submit chama `invoicesController.createCreditCardAdjustment` e atualiza lista/detalhe da fatura.
    - Erro exibe snackbar sem fechar o sheet.
  </behavior>
  <action>Adicionar estado de fatura em reajuste, inputs controlados de valor real/mes/data e um `Sheet` dedicado. Default: `dueMonth` igual ao mes selecionado e data do dia atual; converter data para ISO `T12:00:00.000Z`; converter valor com `currencyInputToDecimal`. Ao sucesso, incrementar `refreshKey`, fechar sheet, limpar estado e notificar `Reajuste de fatura lancado com sucesso.`. Ao erro, manter sheet aberto e notificar `Nao foi possivel lancar o reajuste da fatura.`. Nao adicionar preview detalhado nem tratamento de diferenca zero; isso pertence a Phase 26.</action>
  <verify><automated>npm run test -- tests/e2e/cards-flow.spec.ts</automated></verify>
  <done>Fluxo completo pela tela de cartoes cria reajuste e reflete total atualizado na lista e detalhe da fatura.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cobrir fluxo de reajuste na tela de cartoes</name>
  <files>tests/e2e/cards-flow.spec.ts</files>
  <behavior>
    - Teste cria fatura com total inicial, abre reajuste, informa valor real maior e salva.
    - Tela passa a mostrar total atualizado para o valor real.
    - Detalhe da fatura contem item `REAJUSTE` com amount da diferenca, vinculado ao cartao e dueMonth escolhidos.
  </behavior>
  <action>Adicionar teste e2e em jsdom reaproveitando `CardsPage` e runtime local. Use cartao existente com compra base de `100.00`, reajuste a fatura `2026-03` para `135.00` com data `2026-03-15`, e valide UI + item `REAJUSTE` no detalhe. Se necessario, busque a transacao pelo `transactionsController.listTransactionsByMonth` local e valide `kind: "EXPENSE"`, `amount: "35.00"`, `creditCardId`, `invoiceMonthKey: "2026-03"` e `settlementStatus: null`. Evite depender de texto de preview da Phase 26.</action>
  <verify><automated>npm run test -- tests/e2e/cards-flow.spec.ts</automated></verify>
  <done>Teste prova que o usuario consegue sincronizar uma fatura pela interface e que a transacao gerada fica na fatura correta.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/e2e/cards-flow.spec.ts
- [ ] npm run test -- tests/modules/credit-card-adjustments.service.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
- [ ] Existe botao de reajuste por fatura.
- [ ] Formulario coleta valor real da fatura, mes da fatura e data.
- [ ] Salvar cria transacao `REAJUSTE` e atualiza total da fatura.
- [ ] Fluxos existentes de pagar fatura, desfazer pagamento, adicionar despesa e editar item nao sofrem regressao.
</success_criteria>

<output>
After completion, create `.planning/phases/25-reajuste-de-fatura-de-cartao/25-03-SUMMARY.md`
</output>
