---
phase: 25-reajuste-de-fatura-de-cartao
plan: "02"
type: execute
wave: 2
depends_on:
  - "25-01"
files_modified:
  - src/app/foundation/runtime.ts
  - src/server/vite-api.ts
  - tests/modules/credit-card-adjustments.service.test.ts
  - tests/modules/auth-api.test.ts
autonomous: true
requirements:
  - CCADJ-02
  - CCADJ-03
  - CCADJ-04
  - CCADJ-05
user_setup: []
must_haves:
  truths:
    - Runtime local e runtime API oferecem o mesmo metodo para criar reajuste de fatura.
    - Endpoint autenticado usa sempre o household da sessao e ignora household enviado pelo cliente.
    - Reajuste criado via API/runtime aparece na fatura mensal e nos itens da fatura do cartao.
  artifacts:
    - "src/app/foundation/runtime.ts: contrato `invoicesController.createCreditCardAdjustment`"
    - "src/server/vite-api.ts: rota POST `/api/invoices/adjustment`"
    - "tests/modules/auth-api.test.ts: cobertura leve de autenticacao/isolamento da rota"
  key_links:
    - "src/app/foundation/runtime.ts -> /api/invoices/adjustment via requestSync POST no runtime de API"
    - "src/server/vite-api.ts -> prisma.creditCard/prisma.transaction/prisma.category via ownership, total de fatura e create da transacao"
    - "src/app/foundation/runtime.ts -> src/modules/invoices/credit-card-adjustments.service.ts via runtime local de teste"
---

<objective>
Conectar o reajuste de fatura ao runtime da aplicacao e a API local do Vite.

Purpose: permitir que UI e testes usem um contrato unico para criar reajustes de fatura, com isolamento por sessao no caminho persistido.
Output: metodo em `invoicesController` no runtime, rota POST autenticada e cobertura de integracao leve.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-reajuste-de-fatura-de-cartao/25-01-SUMMARY.md
@src/app/foundation/runtime.ts
@src/server/vite-api.ts
@src/modules/invoices/credit-card-adjustments.service.ts
@src/modules/invoices/invoices.controller.ts
@src/modules/invoices/invoices.service.ts
@tests/modules/auth-api.test.ts
</context>

<interfaces>
New controller contract from Plan 01:
```typescript
createCreditCardAdjustment(input: {
  householdId: string;
  cardId: string;
  realInvoiceTotal: string;
  dueMonth: string;
  occurredAt: string;
}): {
  previousInvoiceTotal: string;
  realInvoiceTotal: string;
  difference: string;
  transaction: TransactionRecord;
}
```
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Ligar reajuste no runtime local e contrato do frontend</name>
  <files>src/app/foundation/runtime.ts, tests/modules/credit-card-adjustments.service.test.ts</files>
  <behavior>
    - `invoicesController.createCreditCardAdjustment` existe no contrato `Runtime`.
    - Em modo teste, o metodo usa `CreditCardAdjustmentsService` com os repositorios locais compartilhados.
    - Depois do reajuste, `invoicesController.getMonthlyInvoices` e `getCardInvoiceEntriesByDueMonth` refletem o valor real informado.
  </behavior>
  <action>Atualizar `InvoicesControllerContract` no runtime para incluir `createCreditCardAdjustment`. No `createLocalRuntime`, instanciar `CreditCardAdjustmentsService` com o mesmo `InvoicesService`, `TransactionsRepository` e `CategoriesRepository` usados pela fundacao. Para evitar duas instancias divergentes, guardar `const invoicesService = new InvoicesService(...)`, criar `const creditCardAdjustmentsService = new CreditCardAdjustmentsService(invoicesService, transactionsRepository, categoriesRepository)`, e passar ambos para `new InvoicesController(invoicesService, creditCardAdjustmentsService)`.</action>
  <verify><automated>npm run test -- tests/modules/credit-card-adjustments.service.test.ts tests/modules/foundation-api.test.ts</automated></verify>
  <done>Fluxo local de teste consegue criar reajuste pela facade `invoicesController` e observar fatura atualizada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar rota persistida para reajuste de fatura</name>
  <files>src/server/vite-api.ts, tests/modules/auth-api.test.ts</files>
  <behavior>
    - `POST /api/invoices/adjustment` exige usuario autenticado.
    - Rota valida que o cartao pertence ao household autenticado.
    - Rota calcula o total atual com a mesma regra de `/api/invoices/items` e `/api/invoices/monthly`.
    - Rota cria categoria `Reajuste` se necessario e transacao `REAJUSTE` na fatura do `dueMonth` informado.
  </behavior>
  <action>Adicionar rota `POST /api/invoices/adjustment` perto das rotas de invoices. A rota deve ler `{ cardId, realInvoiceTotal, dueMonth, occurredAt }`, ignorar qualquer `householdId` do body, validar ownership por `authHouseholdId`, carregar o total atual da fatura via `loadServices().invoicesService.getCardInvoiceEntriesByDueMonth`, calcular `difference = realInvoiceTotal - previousInvoiceTotal`, criar/reusar categoria normalizada `reajuste`, e criar uma transacao Prisma `kind: "EXPENSE"` com `amount` assinado, `creditCardId`, `invoiceMonthKey: dueMonth`, `invoiceDueDate` derivado do vencimento do cartao no dueMonth, `accountId: null`, `settlementStatus: null`, `transferGroupId: null`. Retornar o mesmo formato do servico. Nao aceite `householdId` do body.</action>
  <verify><automated>npm run test -- tests/modules/auth-api.test.ts && npm run lint</automated></verify>
  <done>Endpoint persistido replica a regra do dominio e preserva isolamento por sessao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ligar runtime de API ao endpoint</name>
  <files>src/app/foundation/runtime.ts</files>
  <behavior>
    - Em runtime de API, `invoicesController.createCreditCardAdjustment` envia POST para `/api/invoices/adjustment`.
    - Payload enviado nao inclui `householdId`; a sessao decide o household.
    - Retorno preserva `previousInvoiceTotal`, `realInvoiceTotal`, `difference` e `transaction`.
  </behavior>
  <action>Adicionar implementacao no `createApiRuntime` para chamar `requestSync("POST", "/api/invoices/adjustment", { cardId, realInvoiceTotal, dueMonth, occurredAt })`. Tipar input/output a partir do contrato do runtime, seguindo o padrao ja usado para accounts/cards/transactions/invoices.</action>
  <verify><automated>npm run test -- tests/modules/credit-card-adjustments.service.test.ts && npm run lint</automated></verify>
  <done>O mesmo metodo de runtime funciona em teste e producao sem a UI conhecer detalhes da rota.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/modules/credit-card-adjustments.service.test.ts tests/modules/auth-api.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
- [ ] Runtime expoe `createCreditCardAdjustment` para a UI.
- [ ] Runtime de API chama `/api/invoices/adjustment`.
- [ ] Endpoint persistido calcula diferenca e cria transacao com household autenticado.
- [ ] Fatura mensal e detalhe da fatura mudam para o valor real apos reajuste.
</success_criteria>

<output>
After completion, create `.planning/phases/25-reajuste-de-fatura-de-cartao/25-02-SUMMARY.md`
</output>
