---
phase: 24-reajuste-de-saldo-em-conta
plan: "03"
type: execute
wave: 3
depends_on:
  - "24-02"
files_modified:
  - src/components/foundation/consolidated-balance-card.tsx
  - src/app/foundation/accounts/page.tsx
  - tests/e2e/foundation-flow.spec.ts
autonomous: true
requirements:
  - ACADJ-01
  - ACADJ-05
user_setup: []
must_haves:
  truths:
    - Usuario consegue iniciar reajuste a partir de uma conta visivel na tela de contas.
    - Usuario informa valor real, mes de competencia e data do reajuste.
    - Ao salvar, a tela de contas atualiza o saldo para o valor real informado.
    - A transacao criada fica paga e aparece no mes/data escolhidos.
  artifacts:
    - "src/components/foundation/consolidated-balance-card.tsx: entrada visual para reajuste por conta"
    - "src/app/foundation/accounts/page.tsx: Sheet/formulario de reajuste e chamada ao runtime"
    - "tests/e2e/foundation-flow.spec.ts: cobertura do fluxo de usuario em jsdom"
  key_links:
    - "src/components/foundation/consolidated-balance-card.tsx -> src/app/foundation/accounts/page.tsx via prop `onAdjustAccount(accountId)`"
    - "src/app/foundation/accounts/page.tsx -> src/app/foundation/runtime.ts via `accountsController.createAccountAdjustment`"
    - "tests/e2e/foundation-flow.spec.ts -> src/modules/transactions/transactions.repository.ts via assert em `transactionsController.listTransactionsByMonth`"
---

<objective>
Adicionar a experiencia inicial de reajuste na tela de contas.

Purpose: permitir que o usuario sincronize o saldo de uma conta com a carteira real sem calcular manualmente a diferenca.
Output: botao por conta, sheet de reajuste, chamada ao runtime e teste e2e do fluxo positivo.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-reajuste-de-saldo-em-conta/24-02-SUMMARY.md
@src/components/foundation/consolidated-balance-card.tsx
@src/app/foundation/accounts/page.tsx
@src/app/foundation/runtime.ts
@src/lib/utils.ts
@tests/e2e/foundation-flow.spec.ts
</context>

<interfaces>
Runtime method available after Plan 02:
```typescript
accountsController.createAccountAdjustment({
  householdId,
  accountId,
  realBalance,
  month,
  occurredAt,
});
```

Currency helpers:
```typescript
formatCurrencyInputBRL(raw: string, options?: { allowNegative?: boolean }): string
currencyInputToDecimal(raw: string, options?: { allowNegative?: boolean }): string
```
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Adicionar entrada de reajuste nos cards de conta</name>
  <files>src/components/foundation/consolidated-balance-card.tsx</files>
  <behavior>
    - Cada conta exibe acao acessivel `Reajustar saldo da [conta]`.
    - Acao chama callback com o `account.id`.
    - Botao de objetivo de investimento continua funcionando para contas `INVESTMENT`.
  </behavior>
  <action>Adicionar prop opcional `onAdjustAccount?: (accountId: string) => void` em `ConsolidatedBalanceCard`. Renderizar um botao secundario por conta com `aria-label` especifico e copy curta `Reajustar`, sem criar card dentro de card e preservando layout responsivo atual. Para contas de investimento, manter os botoes de objetivo e reajuste sem sobrepor texto.</action>
  <verify><automated>npm run test -- tests/e2e/foundation-flow.spec.ts</automated></verify>
  <done>Usuario tem ponto de entrada claro para reajustar qualquer conta existente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar sheet de reajuste de conta</name>
  <files>src/app/foundation/accounts/page.tsx</files>
  <behavior>
    - Sheet mostra nome da conta e saldo atual no app.
    - Formulario possui campos `Valor real`, `Mes de competencia` e `Data do reajuste`.
    - Submit chama `accountsController.createAccountAdjustment` e atualiza a tela.
    - Erro exibe snackbar sem fechar o sheet.
  </behavior>
  <action>Adicionar estado de conta em reajuste, inputs controlados de valor real/mes/data e um `Sheet` dedicado. Default: mes/data do dia atual em formato `YYYY-MM` e `YYYY-MM-DD`; converter data para ISO `T12:00:00.000Z`; converter valor com `currencyInputToDecimal`. Ao sucesso, incrementar `refreshKey`, fechar sheet, limpar estado e notificar `Reajuste lancado com sucesso.`. Ao erro, manter sheet aberto e notificar `Nao foi possivel lancar o reajuste.`.</action>
  <verify><automated>npm run test -- tests/e2e/foundation-flow.spec.ts</automated></verify>
  <done>Fluxo completo pela tela de contas cria reajuste e reflete saldo atualizado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cobrir fluxo de reajuste na tela de contas</name>
  <files>tests/e2e/foundation-flow.spec.ts</files>
  <behavior>
    - Teste cria uma conta com saldo inicial, abre reajuste, informa valor real maior e salva.
    - Tela passa a mostrar saldo atualizado para o valor real.
    - `transactionsController.listTransactionsByMonth` contem transacao `REAJUSTE` `INCOME`, amount da diferenca, `settlementStatus: "PAID"` e data no mes escolhido.
  </behavior>
  <action>Adicionar teste e2e em jsdom reaproveitando `AppShell`/runtime local. Use uma conta `CHECKING` com saldo inicial `500.00`, reajuste para `650.00` no mes `2026-04` e data `2026-04-15`, e valide UI + transacao criada. Evite depender de texto de preview da fase 26.</action>
  <verify><automated>npm run test -- tests/e2e/foundation-flow.spec.ts</automated></verify>
  <done>Teste prova que o usuario consegue sincronizar uma conta pela interface e que a transacao gerada tem sinal correto.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/e2e/foundation-flow.spec.ts
- [ ] npm run test -- tests/modules/account-adjustments.service.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
- [ ] Existe botao de reajuste por conta.
- [ ] Formulario coleta valor real, mes e data.
- [ ] Salvar cria transacao `REAJUSTE` e atualiza saldo da conta.
- [ ] Fluxo de objetivo de investimento nao sofre regressao visual ou funcional.
</success_criteria>

<output>
After completion, create `.planning/phases/24-reajuste-de-saldo-em-conta/24-03-SUMMARY.md`
</output>
