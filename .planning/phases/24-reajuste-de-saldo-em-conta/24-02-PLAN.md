---
phase: 24-reajuste-de-saldo-em-conta
plan: "02"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - src/app/foundation/runtime.ts
  - src/server/vite-api.ts
  - tests/modules/account-adjustments.service.test.ts
autonomous: true
requirements:
  - ACADJ-01
  - ACADJ-02
  - ACADJ-03
  - ACADJ-04
  - ACADJ-05
  - ADJ-04
user_setup: []
must_haves:
  truths:
    - Runtime local e runtime API oferecem o mesmo metodo para criar reajuste de conta.
    - Endpoint autenticado usa sempre o household da sessao e ignora household enviado pelo cliente.
    - Reajuste criado via API/runtime atualiza o saldo consolidado da conta.
  artifacts:
    - "src/app/foundation/runtime.ts: contrato de frontend para `accountsController.createAccountAdjustment`"
    - "src/server/vite-api.ts: rota POST `/api/accounts/adjustment`"
  key_links:
    - "src/app/foundation/runtime.ts -> /api/accounts/adjustment via requestSync POST no runtime de API"
    - "src/server/vite-api.ts -> prisma.account/prisma.transaction/prisma.category via validacao de ownership, calculo de saldo e create da transacao"
    - "src/app/foundation/runtime.ts -> src/modules/accounts/account-adjustments.service.ts via runtime local de teste usando o servico do plano 01"
---

<objective>
Conectar o reajuste de conta ao runtime da aplicacao e a API local do Vite.

Purpose: permitir que UI e testes usem um contrato unico para criar reajustes, com isolamento por sessao no caminho persistido.
Output: metodo em `accountsController` no runtime, rota POST autenticada e cobertura de integracao leve.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-reajuste-de-saldo-em-conta/24-01-SUMMARY.md
@src/app/foundation/runtime.ts
@src/server/vite-api.ts
@src/modules/accounts/account-adjustments.service.ts
@src/modules/accounts/accounts.controller.ts
</context>

<interfaces>
New controller contract from Plan 01:
```typescript
createAccountAdjustment(input: {
  householdId: string;
  accountId: string;
  realBalance: string;
  month: string;
  occurredAt: string;
}): {
  previousBalance: string;
  realBalance: string;
  difference: string;
  transaction: TransactionRecord;
}
```
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Ligar reajuste no runtime local e contrato do frontend</name>
  <files>src/app/foundation/runtime.ts, tests/modules/account-adjustments.service.test.ts</files>
  <behavior>
    - `accountsController.createAccountAdjustment` existe no contrato `Runtime`.
    - Em modo teste, o metodo usa `AccountAdjustmentsService` com os repositorios locais compartilhados.
    - Depois do reajuste, `accountsController.getConsolidatedBalance` reflete o valor real informado.
  </behavior>
  <action>Atualizar `AccountsControllerContract` no runtime para incluir `createAccountAdjustment`. No `createLocalRuntime`, instanciar `AccountAdjustmentsService` com o mesmo `AccountsService`, `TransactionsRepository` e `CategoriesRepository` usados pelo restante da fundacao, e expor o metodo por `accountsController`. Manter os demais metodos e contratos sem regressao.</action>
  <verify><automated>npm run test -- tests/modules/account-adjustments.service.test.ts tests/modules/foundation-api.test.ts</automated></verify>
  <done>Fluxo local de teste consegue criar reajuste pela facade `accountsController` e observar saldo consolidado atualizado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar rota persistida para reajuste de conta</name>
  <files>src/server/vite-api.ts</files>
  <behavior>
    - `POST /api/accounts/adjustment` exige usuario autenticado.
    - Rota valida que a conta pertence ao household autenticado.
    - Rota calcula o saldo atual com a mesma regra de `/api/accounts/consolidated`.
    - Rota cria categoria `Reajuste` se necessario e transacao `REAJUSTE` paga com sinal correto.
  </behavior>
  <action>Adicionar rota `POST /api/accounts/adjustment` depois das rotas de conta. A rota deve ler `{ accountId, realBalance, month, occurredAt }`, validar `occurredAt.slice(0, 7) === month`, carregar a conta por id e household da sessao, calcular saldo atual incluindo transacoes pagas, faturas quitadas e instancias pagas, criar/reusar categoria normalizada `reajuste`, criar transacao `INCOME` ou `EXPENSE` com amount absoluto e retornar o mesmo formato do servico. Nao aceite `householdId` do body no caminho de API.</action>
  <verify><automated>npm run lint</automated></verify>
  <done>Endpoint persistido replica a regra do dominio e preserva isolamento por sessao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ligar runtime de API ao endpoint</name>
  <files>src/app/foundation/runtime.ts</files>
  <behavior>
    - Em runtime de API, `accountsController.createAccountAdjustment` envia POST para `/api/accounts/adjustment`.
    - Payload enviado nao inclui `householdId`; a sessao decide o household.
    - Retorno preserva `previousBalance`, `realBalance`, `difference` e `transaction`.
  </behavior>
  <action>Adicionar implementacao no `createApiRuntime` para chamar `requestSync("POST", "/api/accounts/adjustment", { accountId, realBalance, month, occurredAt })`. Tipar input/output a partir do contrato do runtime, seguindo o padrao ja usado para accounts/cards/transactions.</action>
  <verify><automated>npm run lint</automated></verify>
  <done>O mesmo metodo de runtime funciona em teste e producao sem a UI conhecer detalhes da rota.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/modules/account-adjustments.service.test.ts tests/modules/foundation-api.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
- [ ] Runtime expoe `createAccountAdjustment` para a UI.
- [ ] Runtime de API chama `/api/accounts/adjustment`.
- [ ] Endpoint persistido calcula diferenca e cria transacao com household autenticado.
- [ ] Saldo consolidado muda para o valor real apos reajuste.
</success_criteria>

<output>
After completion, create `.planning/phases/24-reajuste-de-saldo-em-conta/24-02-SUMMARY.md`
</output>
