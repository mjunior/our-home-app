---
phase: 24-reajuste-de-saldo-em-conta
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/accounts/accounts.service.ts
  - src/modules/accounts/accounts.controller.ts
  - src/modules/accounts/account-adjustments.service.ts
  - src/modules/categories/categories.repository.ts
  - tests/modules/account-adjustments.service.test.ts
autonomous: true
requirements:
  - ACADJ-02
  - ACADJ-03
  - ACADJ-04
  - ACADJ-05
  - ADJ-04
user_setup: []
must_haves:
  truths:
    - Usuario informa um valor real de conta e o dominio calcula a diferenca contra o saldo atual do app.
    - Diferenca positiva gera lancamento `REAJUSTE` de entrada na conta.
    - Diferenca negativa gera lancamento `REAJUSTE` de saida na conta.
    - Lancamento criado iguala o saldo consolidado da conta ao valor real informado.
    - Conta de outro household nao pode ser reajustada.
  artifacts:
    - "src/modules/accounts/account-adjustments.service.ts: servico de dominio para calcular e gravar reajuste de conta"
    - "src/modules/accounts/accounts.service.ts: snapshot reutilizavel de saldo atual por conta"
    - "tests/modules/account-adjustments.service.test.ts: cobertura positiva, negativa e ownership"
  key_links:
    - "src/modules/accounts/account-adjustments.service.ts -> src/modules/accounts/accounts.service.ts via consulta de saldo atual antes do calculo da diferenca"
    - "src/modules/accounts/account-adjustments.service.ts -> src/modules/transactions/transactions.repository.ts via create de transacao INCOME/EXPENSE com description `REAJUSTE`"
    - "src/modules/accounts/account-adjustments.service.ts -> src/modules/categories/categories.repository.ts via categoria sistemica `Reajuste` criada ou reutilizada dentro do household"
---

<objective>
Criar a base de dominio para reajustar saldo de conta a partir do valor real informado.

Purpose: garantir que o sistema calcule o sinal e o valor do reajuste, em vez de exigir que o usuario lance manualmente a diferenca.
Output: servico de reajuste de conta, contrato no controller, helper de saldo atual e testes de dominio.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/modules/accounts/accounts.service.ts
@src/modules/accounts/accounts.controller.ts
@src/modules/accounts/accounts.repository.ts
@src/modules/categories/categories.repository.ts
@src/modules/transactions/transactions.repository.ts
@src/domain/shared/money.ts
</context>

<interfaces>
Existing account balance behavior:
```typescript
export class AccountsService {
  consolidatedBalance(householdId: string): {
    amount: string;
    byType: { CHECKING: string; INVESTMENT: string };
    accounts: Array<{ id: string; name: string; type: "CHECKING" | "INVESTMENT"; balance: string }>;
  }
}
```

Existing transaction record shape:
```typescript
export interface TransactionRecord {
  kind: "INCOME" | "EXPENSE";
  description: string;
  amount: string;
  occurredAt: string;
  accountId: string | null;
  settlementStatus: "PAID" | "UNPAID" | null;
}
```
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extrair snapshot de saldo atual por conta</name>
  <files>src/modules/accounts/accounts.service.ts</files>
  <behavior>
    - `getAccountBalanceSnapshot({ householdId, accountId })` retorna a conta e o saldo calculado com a mesma regra de `consolidatedBalance`.
    - Conta inexistente ou de outro household dispara `ACCOUNT_NOT_FOUND`.
    - `consolidatedBalance` continua retornando o mesmo payload atual.
  </behavior>
  <action>Adicionar um metodo publico em `AccountsService` para obter o saldo atual de uma conta especifica, reutilizando a mesma composicao ja usada no consolidado: saldo inicial + transacoes de conta `PAID` - faturas quitadas + instancias agendadas `PAID`. Evite duplicar regras divergentes; extraia helper privado se necessario e mantenha o formato de dinheiro com duas casas decimais.</action>
  <verify><automated>npm run test -- tests/modules/foundation-api.test.ts</automated></verify>
  <done>O saldo individual usado pelo reajuste e igual ao saldo exibido no consolidado, incluindo movimentos pagos, faturas quitadas e instancias pagas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar servico de reajuste de conta</name>
  <files>src/modules/accounts/account-adjustments.service.ts, src/modules/categories/categories.repository.ts, tests/modules/account-adjustments.service.test.ts</files>
  <behavior>
    - Valor real maior que saldo atual cria transacao `INCOME` com description `REAJUSTE` e amount absoluto da diferenca.
    - Valor real menor que saldo atual cria transacao `EXPENSE` com description `REAJUSTE` e amount absoluto da diferenca.
    - Data do lancamento deve pertencer ao mes de competencia informado; divergencia dispara `ACCOUNT_ADJUSTMENT_MONTH_MISMATCH`.
    - Conta de outro household dispara `ACCOUNT_NOT_FOUND` e nao cria transacao.
  </behavior>
  <action>Criar `AccountAdjustmentsService` com input `{ householdId, accountId, realBalance, month, occurredAt }`. O servico deve buscar o saldo via `AccountsService.getAccountBalanceSnapshot`, calcular `difference = realBalance - currentBalance`, criar ou reutilizar categoria sistemica `Reajuste` no mesmo household via novo metodo `CategoriesRepository.findByNormalized(householdId, normalized)`, e gravar uma transacao na conta: `INCOME` para diferenca positiva, `EXPENSE` para negativa, `amount` absoluto em `toFixed(2)`, `description: "REAJUSTE"`, `settlementStatus: "PAID"`, `invoiceMonthKey: null`, `invoiceDueDate: null`, `transferGroupId: null`. Retornar `{ previousBalance, realBalance, difference, transaction }`.</action>
  <verify><automated>npm run test -- tests/modules/account-adjustments.service.test.ts</automated></verify>
  <done>Testes demonstram reajuste positivo, reajuste negativo, validacao de mes/data e bloqueio por ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expor contrato de reajuste no controller de contas</name>
  <files>src/modules/accounts/accounts.controller.ts, tests/modules/account-adjustments.service.test.ts</files>
  <behavior>
    - `AccountsController.createAccountAdjustment(payload)` delega ao novo servico.
    - O retorno do controller preserva `previousBalance`, `realBalance`, `difference` e `transaction`.
  </behavior>
  <action>Atualizar `AccountsController` para receber opcionalmente um `AccountAdjustmentsService` no construtor e expor `createAccountAdjustment`. Para preservar compatibilidade dos testes existentes, deixe o parametro opcional e lance erro claro `ACCOUNT_ADJUSTMENT_SERVICE_NOT_CONFIGURED` se o metodo for chamado sem o servico.</action>
  <verify><automated>npm run test -- tests/modules/account-adjustments.service.test.ts tests/modules/foundation-api.test.ts</automated></verify>
  <done>Controller oferece o contrato consumivel por runtime/API sem quebrar criacao, listagem, objetivo de investimento ou saldo consolidado.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/modules/account-adjustments.service.test.ts
- [ ] npm run test -- tests/modules/foundation-api.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
- [ ] Saldo atual usado no reajuste e o mesmo saldo exibido no modulo de contas.
- [ ] Reajuste positivo cria entrada `REAJUSTE` paga na conta.
- [ ] Reajuste negativo cria saida `REAJUSTE` paga na conta.
- [ ] Reajuste respeita household, conta, data e mes informado.
</success_criteria>

<output>
After completion, create `.planning/phases/24-reajuste-de-saldo-em-conta/24-01-SUMMARY.md`
</output>
