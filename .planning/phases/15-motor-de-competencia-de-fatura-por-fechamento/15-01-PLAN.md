---
phase: 15-motor-de-competencia-de-fatura-por-fechamento
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/invoices/invoice-cycle.service.ts
  - tests/modules/invoice-cycle.service.test.ts
  - tests/modules/invoices.service.test.ts
autonomous: true
requirements:
  - CCB-01
  - CCB-02
user_setup: []
must_haves:
  truths:
    - Regra de fechamento passa a ser inclusiva no dia de corte (`day >= closeDay` vai para proxima competencia).
    - Exemplo de referencia do usuario (close 05 / due 10) e reproduzido pelos testes de ciclo.
    - Calculo de `invoiceMonthKey` e `dueDate` fica deterministico para qualquer cartao.
  artifacts:
    - src/modules/invoices/invoice-cycle.service.ts
    - tests/modules/invoice-cycle.service.test.ts
  key_links:
    - Motor de ciclo unificado evita divergencia de regra entre modulos.
    - Fase 16 depende desta regra correta para consolidar fatura no cashflow.
---

<objective>
Implementar e validar a regra de competencia de fatura com corte inclusivo no fechamento.

Purpose: alinhar o motor de ciclo de cartao ao comportamento real esperado pelo usuario.
Output: `resolveExpenseCycle` refletindo `day < closeDay` (mes atual) e `day >= closeDay` (mes seguinte), com testes de regressao.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-motor-de-competencia-de-fatura-por-fechamento/15-CONTEXT.md
@src/modules/invoices/invoice-cycle.service.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Ajustar regra de corte inclusiva no InvoiceCycleService</name>
  <files>src/modules/invoices/invoice-cycle.service.ts, tests/modules/invoice-cycle.service.test.ts</files>
  <behavior>
    - `day < closeDay` permanece na competencia corrente.
    - `day >= closeDay` migra para competencia seguinte.
    - `dueDate` continua derivado de `dueDay` para a competencia resolvida.
  </behavior>
  <action>Refatorar `resolveExpenseCycle` e `resolveCurrentAndNext` para usar comparacao inclusiva no fechamento, mantendo API publica do servico e sem alterar contratos externos.</action>
  <verify><automated>npm run test -- tests/modules/invoice-cycle.service.test.ts</automated></verify>
  <done>Motor de ciclo atualizado com corte inclusivo e testes unitarios verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cobrir exemplos reais de fechamento/vencimento do usuario</name>
  <files>tests/modules/invoice-cycle.service.test.ts, tests/modules/invoices.service.test.ts</files>
  <behavior>
    - Cenarios informados pelo usuario (05/03, 06/03, 10/03, 01/04, 04/04, 05/04, 06/04) batem com vencimentos esperados.
    - Nenhum caso de borda no dia do fechamento regressa para o comportamento antigo.
  </behavior>
  <action>Adicionar casos de teste orientados a tabela reproduzindo o calendario do usuario para garantir previsibilidade do ciclo.</action>
  <verify><automated>npm run test -- tests/modules/invoice-cycle.service.test.ts tests/modules/invoices.service.test.ts</automated></verify>
  <done>Exemplos de negocio validados e protegidos por regressao automatizada.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/modules/invoice-cycle.service.test.ts tests/modules/invoices.service.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
- CCB-01 e CCB-02 atendidos com evidencia automatizada.
- Regra de fechamento inclusiva confirmada em testes de dominio.
</success_criteria>

<output>
After completion, create `.planning/phases/15-motor-de-competencia-de-fatura-por-fechamento/15-01-SUMMARY.md`
</output>
