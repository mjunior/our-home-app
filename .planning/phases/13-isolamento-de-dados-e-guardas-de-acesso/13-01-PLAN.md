---
phase: 13-isolamento-de-dados-e-guardas-de-acesso
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/vite-api.ts
  - src/modules/auth/session-token.ts
  - src/modules/auth/auth.service.ts
  - tests/modules/auth-api.test.ts
  - tests/modules/foundation-api.test.ts
autonomous: true
requirements:
  - SECU-01
  - SECU-04
user_setup: []
must_haves:
  truths:
    - Toda rota privada retorna `401` quando nao existe sessao valida.
    - Rotas publicas continuam limitadas a `/login` e `/n-account` no fluxo atual de auth.
    - Erros de acesso negado sao neutros e nao vazam informacao sensivel.
  artifacts:
    - src/server/vite-api.ts
    - tests/modules/auth-api.test.ts
    - tests/modules/foundation-api.test.ts
  key_links:
    - Middleware unico valida cookie/JWT e injeta contexto autenticado antes dos handlers privados.
    - Handlers privados usam helper de auth para padronizar `401` e mensagens neutras.
---

<objective>
Implementar guarda global de autenticacao para todas as rotas privadas e padronizar contrato de erro de acesso.

Purpose: estabelecer fronteira de seguranca fail-closed para impedir acesso anonimo fora das rotas publicas de auth.
Output: middleware de sessao aplicado em lote e cobertura automatizada de `401` nas rotas privadas.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-isolamento-de-dados-e-guardas-de-acesso/13-CONTEXT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Criar middleware unico de auth para rotas privadas com contrato `401`</name>
  <files>src/server/vite-api.ts, tests/modules/auth-api.test.ts</files>
  <behavior>
    - Sem cookie/sessao valida, rota privada retorna `401 AUTH_UNAUTHENTICATED`.
    - Rotas de auth publico (`/api/auth/register`, `/api/auth/login`, `/api/auth/logout`, `/api/auth/me`) seguem funcionando.
    - Guardas sao aplicados de forma centralizada (nao por endpoint isolado).
  </behavior>
  <action>Refatorar o middleware de API para classificar rotas publicas vs privadas e aplicar validacao de sessao antes de qualquer handler privado. Extrair helper de contexto autenticado para evitar duplicacao de parse/verify JWT e manter resposta neutra em falhas de auth.</action>
  <verify><automated>npm run test -- tests/modules/auth-api.test.ts</automated></verify>
  <done>Guardas globais estao ativos e todas as rotas privadas falham com `401` sem sessao.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cobrir regressao de guardas nas APIs foundation existentes</name>
  <files>tests/modules/foundation-api.test.ts, src/server/vite-api.ts</files>
  <behavior>
    - Endpoints financeiros sem sessao nao retornam dados.
    - Respostas de erro permanecem neutras sem leakage de recurso.
  </behavior>
  <action>Adicionar casos de teste para principais endpoints foundation (accounts/cards/categories/transactions/schedules/free-balance) garantindo bloqueio anonimo e contrato de erro consistente apos habilitacao da guarda global.</action>
  <verify><automated>npm run test -- tests/modules/foundation-api.test.ts tests/modules/auth-api.test.ts</automated></verify>
  <done>Regressao de acesso anonimo bloqueado esta coberta nos endpoints privados principais.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/modules/auth-api.test.ts tests/modules/foundation-api.test.ts
- [ ] npm run lint
</verification>

<success_criteria>
- SECU-01 coberto com guardas globais retornando `401` para anonimos.
- Base de erro neutro de autorizacao preparada para SECU-04.
</success_criteria>

<output>
After completion, create `.planning/phases/13-isolamento-de-dados-e-guardas-de-acesso/13-01-SUMMARY.md`
</output>
