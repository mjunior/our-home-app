---
phase: 14-ux-de-login-e-cadastro-isolado
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.tsx
  - src/app/auth/login-page.tsx
  - src/app/foundation/runtime.ts
  - index.html
  - tests/e2e/auth-flow.spec.ts
autonomous: true
requirements:
  - UX-01
  - UX-03
user_setup: []
must_haves:
  truths:
    - A rota `/` passa a ser entrada principal anonima e exibe login.
    - Usuario sem sessao autenticada nunca acessa shell privada fora de `/` e `/n-account`.
    - `/` e `/login` aplicam noindex sem depender de backend externo.
  artifacts:
    - src/main.tsx
    - src/app/auth/login-page.tsx
    - index.html
  key_links:
    - Bootstrap de sessao decide acesso anonimo/autenticado com `root login-first`.
    - Meta robots noindex e aplicada para rotas anonimas (`/` e `/login`).
---

<objective>
Implementar entrada login-first pela raiz e hardening de indexacao para rotas anonimas.

Purpose: fechar gaps de fluxo inicial e SEO de auth apontados na auditoria da milestone.
Output: root `/` operacional como login anonimo e metadados noindex confiaveis.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-ux-de-login-e-cadastro-isolado/14-CONTEXT.md
@.planning/v1.3-MILESTONE-AUDIT.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Tornar `/` a entrada principal de login anonimo</name>
  <files>src/main.tsx, src/app/auth/login-page.tsx, tests/e2e/auth-flow.spec.ts</files>
  <behavior>
    - Usuario anonimo em `/` visualiza tela de login.
    - Usuario autenticado em `/` entra direto no app shell.
    - Fluxo legacy `/login` continua funcional durante transicao.
  </behavior>
  <action>Refatorar a regra de roteamento no bootstrap para priorizar `/` como entrada default para anonimos, preservando redirecionamentos de sessao e evitando loops de navegacao.</action>
  <verify><automated>npm run test -- tests/e2e/auth-flow.spec.ts</automated></verify>
  <done>Entrada login-first pela raiz implementada e validada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aplicar noindex em `/` e `/login` com cobertura de regressao</name>
  <files>src/main.tsx, index.html, tests/e2e/auth-flow.spec.ts</files>
  <behavior>
    - Rotas anonimas `/` e `/login` expõem meta robots `noindex`.
    - Rotas autenticadas nao sofrem efeito colateral de metadados anonimos.
  </behavior>
  <action>Adicionar estrategia de metadados no frontend (meta robots dinamica e/ou fallback estatico) para garantir noindex em fluxos anonimos, incluindo testes de presenca e alternancia de estado.</action>
  <verify><automated>npm run test -- tests/e2e/auth-flow.spec.ts</automated></verify>
  <done>Hardening noindex aplicado e coberto por teste.</done>
</task>
</tasks>

<verification>
- [ ] npm run test -- tests/e2e/auth-flow.spec.ts
- [ ] npm run lint
</verification>

<success_criteria>
- UX-01 atendido com `/` funcionando como entrada principal anonima.
- UX-03 atendido com noindex validado em `/` e `/login`.
</success_criteria>

<output>
After completion, create `.planning/phases/14-ux-de-login-e-cadastro-isolado/14-01-SUMMARY.md`
</output>
